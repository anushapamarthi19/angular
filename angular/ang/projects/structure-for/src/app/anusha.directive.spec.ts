import { AnushaDirective } from './anusha.directive';

describe('AnushaDirective', () => {
  it('should create an instance', () => {
    const directive = new AnushaDirective();
    expect(directive).toBeTruthy();
  });
});
